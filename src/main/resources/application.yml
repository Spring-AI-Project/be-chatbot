spring:
  application:
    name: ai-project
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
ollama:
  base-url: http://localhost:11434
  model: llama3.2:latest

jwt:
  secret: "my-secret-key-for-jwt-signing-should-be-long"
  expiration: 86400000  # 24??


